using UnityEngine;

public class Gym_CardInHandView : MonoBehaviour
{
    // have a root layer that control position/rotation/size of the card
    // Handle inputs with a front layer that block raycast on images of the card
        // need a tool to handle all inputs
    // contain card data
    // card 

    public enum CardInHandState
    {
        ENABLE,
        DISABLE
    }

    public enum CardInHandFlowState
    {
        IDLE,


    }



}
